server:
  port: 60000

spring:
  application:
    name: user-service
  config:
    import: "configserver:http://127.0.0.1:8888"
  cloud:
    config:
      name: actuator,eureka,rabbitmq,token,logging,zipkin,database

greeting:
  message: Welcome to the Simple E-commerce.

gateway:
  ip: localhost

logging:
  level:
    com.example.userservice: DEBUG